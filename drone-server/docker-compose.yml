version: "3.7"

services:
  server:
    image: golang
    env_file:
        - secrets.env
    command: >
        bash -c "
            /build/scripts/build.sh
        "
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./build:/build
    ports:
      - "8080:8080"

